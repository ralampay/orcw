<%= render 'refinery/admin/search_header', :url => refinery.downloadable_forms_admin_downloadable_forms_path %>
<div class='pagination_container'>
  <% if @downloadable_forms.any? %>
    <%= render 'downloadable_forms' %>
  <% else %>
    <p>
      <% if searching? %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% else %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% end %>
    </p>
  <% end %>
</div>
